input {
    udp {
        port => "5555"
    }
}

filter {

}

output {
 elasticsearch {
   index => "prout-%{+YYYY.MM.dd}"
   hosts => ["https://es01:9200"]
   user => "elastic"
   password => "changeme"
   ssl_enabled => true
   ssl_certificate_authorities => "/usr/share/logstash/certs/ca/ca.crt"
    }
 stdout {
    
 }
}
