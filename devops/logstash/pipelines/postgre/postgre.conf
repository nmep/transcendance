input {
    jdbc {
        jdbc_connection_string => "jdbc:postgresql://db:5432/toto_db"
        jdbc_user => "toto"
        jdbc_password => "password"
        jdbc_driver_class => "org.postgresql.Driver"
        schedule => "0 * * * *" # cronjob schedule format (see "Helpful Links")
        jdbc_paging_enabled => "true"
        statement => "SELECT *" # the PG command for retrieving the documents IMPORTANT: no semicolon!
        jdbc_page_size => "300"
    }
}



output {
if [uri] != "/metrics" {
 elasticsearch {
   index => "auth-%{+YYYY.MM.dd}"
   hosts => ["https://es01:9200"]
   user => "elastic"
   password => "changeme"
   ssl_enabled => true
   ssl_certificate_authorities => "/usr/share/logstash/certs/ca/ca.crt"
 }
 stdout {}
}
}